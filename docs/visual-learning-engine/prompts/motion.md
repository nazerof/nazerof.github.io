# Motion DSL generation prompt

Generate one JSON document that validates against `schemas/visuals/motion.schema.json`.

Use seconds and create a 20–25 second explanation: ten flows run smoothly; fifty and then more than two hundred target 06:00; capacity fills; queues and timeouts grow; retries cascade; staggered schedules, backoff with jitter, and isolation restore stable throughput. Reference only approved learning objectives, concepts, visual IDs, and diagram node IDs.

Use only schema-enumerated stages, easing, and entry presets. Include a complete reduced-motion equivalent. Do not output JavaScript, animation functions, HTML, CSS, URLs, imports, JSX, or unsupported fields. Return JSON only.
